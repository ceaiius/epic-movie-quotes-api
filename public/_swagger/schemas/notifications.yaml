components:
  schemas:
    Notifications:
      allOf:
        - $ref: "../common.yaml#/components/schemas/Id"
        - type: object
          properties:
            for_id:
              type: integer
              example: 1
            from_id:
              type: integer
              example: 2
            quotes_id:
              type: integer
              example: 1
            type:
              type: string
              example: "comment"
            read: 
              type: integer
              example: 0
                   
        - $ref: "../common.yaml#/components/schemas/TimeStamps"